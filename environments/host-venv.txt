# Sonora host venv (.venv) — the environment that derives corpora and runs the
# synthesis/audit scripts. Recorded 2026-08-06, refreshed 2026-08-19 from the live venv (T4/G-1).
#
# This is the venv that produced libritts_r_vat v1..v3c and every render campaign,
# and until now it was recorded nowhere. NOT a resolver output — a snapshot of what
# actually ran, which is the only thing that can be checked against an artifact.
# Deliberately NO torch: the host lane is espeak-free CPU tooling (op_g2p is a
# TFLite graph), and every model-touching job runs in the ROCm container.
#
# Rebuild:  uv venv && uv pip install --python .venv/bin/python -r environments/host-venv.txt
#           (⚠ the path said `env/` until 2026-08-19 — issue #157. The directory is
#            `environments/`, and the command as written could never have run.)
# Refresh:  uv pip freeze --python .venv/bin/python  (keep this header)
#
# Scripts run as .venv/bin/python, never `uv run` — uv run binds to pyproject.toml
# and ignores the inline PEP 723 blocks the scripts carry (owner decision 2026-08-01).

#
# ⚠ REFRESHED 2026-08-19 BECAUSE A DEPENDENCY CHANGED AND THIS FILE DID NOT (issue #153).
# The `pydantic-boundaries` branch added `pydantic>=2` to [project.dependencies] and left
# both snapshots alone, which is the exact case the Refreshing section of README.md names:
# "Refresh both after any dependency change… a stale snapshot asserting the wrong versions
# is the failure mode these files exist to prevent." Nothing enforced it — `grep -rn
# environments/ tests/` returns no matches — so the rule had no mechanism and the review
# that caught it was looking at a diff, not at this file.
#
# The added lines are the pydantic chain plus fastapi/starlette. ⚠ NOT the `vocalizer`
# extra, which the first version of this note claimed (issue #155): that extra is
# `gradio, fastapi, uvicorn, starlette`, and NEITHER gradio NOR uvicorn is installed here.
# What is installed is `fastapi`, `pysbd` and `pytest` — the `dev` extra. This file records
# WHAT ACTUALLY RAN rather than what the resolver would pick, per the header above, and the
# note explaining it is held to the same standard.
ai-edge-litert==2.1.6
annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.14.2
audioread==3.1.0
av==18.0.0
backports-strenum==1.3.1
beautifulsoup4==4.15.0
certifi==2026.7.22
cffi==2.1.0
charset-normalizer==3.4.9
click==8.4.2
ctranslate2==4.8.1
decorator==5.3.1
ebooklib==0.20
fastapi==0.141.1
faster-whisper==1.2.1
filelock==3.32.2
flatbuffers==25.12.19
fsspec==2026.7.0
h11==0.16.0
hf-xet==1.5.2
httpcore==1.0.9
httpx==0.28.1
huggingface-hub==1.26.0
idna==3.18
iniconfig==2.3.0
joblib==1.5.3
lazy-loader==0.5
librosa==0.11.0
llvmlite==0.48.0
lxml==6.1.1
ml-dtypes==0.5.4
msgpack==1.2.1
narwhals==2.24.0
numba==0.66.0
numpy==2.4.6
onnxruntime==1.28.0
packaging==26.2
platformdirs==4.11.0
pluggy==1.6.0
pooch==1.9.0
protobuf==7.35.1
pycparser==3.0
pydantic==2.13.4
pydantic-core==2.46.4
pygments==2.20.0
pyloudnorm==0.2.0
pysbd==0.3.4
pytest==9.1.1
pyyaml==6.0.3
requests==2.34.2
scikit-learn==1.9.0
scipy==1.17.1
setuptools==83.0.0
six==1.17.0
soundfile==0.14.0
soupsieve==2.9.1
soxr==1.1.0
starlette==1.6.0
threadpoolctl==3.6.0
tokenizers==0.23.1
tqdm==4.70.0
typing-extensions==4.16.0
typing-inspection==0.4.2
unidecode==1.4.0
urllib3==2.7.0
